使用python 3.11进行后端开发，使用fastapi框架，使用sqlalchemy进行数据库操作，使用sqlite数据库

使用 uv 管理依赖，使用 uv 创建虚拟环境，使用 requirements.txt 管理依赖，如果需要安装新的依赖，安装后更新 requirements.txt 文件

不要有过长的单个文件，可以划分成多个小文件进行依赖、导入等。

较为复杂的逻辑以及较长的函数要有合理的注释。