# 接口文档

## 通用约定

- 字段名使用小驼峰。
- ID 字段中的 `ID` 全部大写，例如 `userID`、`robotID`、`toneID`。
- 时间戳使用 13 位毫秒时间戳。
- 除登录接口外，token 通过请求头传递：

```http
Authorization: Bearer <token>
```

- 统一响应结构：

```json
{
  "code": 200,
  "data": {},
  "msg": "ok"
}
```

## 权限约定

- token 中包含当前登录用户的 `userID`。
- 管理员由 `users.is_admin` 判断：
  - `0`：普通用户
  - `1`：管理员
- 管理员调用 `GET /api/user/{userID}/robot` 时返回所有未删除机器人。
- 普通用户调用 `GET /api/user/{userID}/robot` 时只返回自己绑定的机器人。
- 管理员可以读取和修改任意机器人的人格、音色、消息、摘要、用户画像和家庭画像。
- 普通用户访问机器人数据接口时，必须已绑定对应 `robotID`。
- 普通用户未绑定对应机器人时，机器人数据接口返回：

```json
{
  "code": 403,
  "data": null,
  "msg": "权限不足：当前用户未绑定该机器人"
}
```

- 以下接口用于绑定前选择，只要求 token 有效，不校验用户是否已绑定机器人：
  - `GET /api/robot/{robotID}/tone`
  - `GET /api/robot/{robotID}/initPersonality`
  - `GET /api/robot/{robotID}/skill`

## 用户接口

### 用户注册 / 登录

路由：`POST /api/user/token`

是否需要 token：否

请求体：

```json
{
  "phonenumber": "18800000000",
  "code": "123456"
}
```

返回体：

```json
{
  "code": 200,
  "data": {
    "token": "jwt-token",
    "userID": "user-uuid",
    "isAdmin": 0
  },
  "msg": "ok"
}
```

### 绑定机器人

路由：`POST /api/user/robot/bind`

是否需要 token：是

请求体：

```json
{
  "userID": "user-uuid",
  "robotID": "ROBOT-DEMO-001",
  "personalityID": 1,
  "toneID": 1
}
```

返回体：

```json
{
  "code": 200,
  "data": null,
  "msg": "ok"
}
```

### 解绑机器人

路由：`DELETE /api/user/robot/unbind`

是否需要 token：是

请求体：

```json
{
  "userID": "user-uuid",
  "robotID": "ROBOT-DEMO-001"
}
```

返回体：

```json
{
  "code": 200,
  "data": null,
  "msg": "ok"
}
```

### 修改机器人别名

路由：`PUT /api/user/robot/alias`

是否需要 token：是

请求体：

```json
{
  "userID": "user-uuid",
  "robotID": "ROBOT-DEMO-001",
  "robotAlias": "新的机器人别名"
}
```

返回体：

```json
{
  "code": 200,
  "data": null,
  "msg": "ok"
}
```

### 获取用户信息

路由：`GET /api/user/{userID}/info`

是否需要 token：是

返回体：

```json
{
  "code": 200,
  "data": {
    "phonenumber": "18800000000",
    "userID": "user-uuid",
    "userName": "用户名",
    "avatar": "头像路径",
    "isAdmin": 0
  },
  "msg": "ok"
}
```

### 修改用户信息

路由：`PUT /api/user/{userID}/info/change`

是否需要 token：是

请求体：

```json
{
  "phonenumber": "18800000000",
  "userName": "用户名",
  "avatar": "头像路径",
  "code": "123456"
}
```

说明：请求体字段均为可选字段，不传则不修改。

返回体：

```json
{
  "code": 200,
  "data": null,
  "msg": "ok"
}
```

### 获取用户机器人列表

路由：`GET /api/user/{userID}/robot`

是否需要 token：是

权限说明：

- 管理员返回所有未删除机器人。
- 普通用户返回自己的未删除绑定机器人。

返回体：

```json
{
  "code": 200,
  "data": {
    "robotList": [
      {
        "robotCode": "ROBOT-DEMO-001",
        "robotName": "机器人名称或用户别名"
      }
    ]
  },
  "msg": "ok"
}
```

## 机器人选项接口

这些接口用于绑定前展示候选项，只要求 token 有效，不校验用户是否已绑定 `robotID`。

### 获取全部音色

路由：`GET /api/robot/{robotID}/tone`

是否需要 token：是

返回体：

```json
{
  "code": 200,
  "data": {
    "toneList": [
      {
        "toneID": 1,
        "toneName": "音色名称"
      }
    ]
  },
  "msg": "ok"
}
```

### 获取全部初始人格

路由：`GET /api/robot/{robotID}/initPersonality`

是否需要 token：是

返回体：

```json
{
  "code": 200,
  "data": {
    "personalityList": [
      {
        "personalityID": 1,
        "personalityName": "人格名称"
      }
    ]
  },
  "msg": "ok"
}
```

### 获取全部技能

路由：`GET /api/robot/{robotID}/skill`

是否需要 token：是

返回体：

```json
{
  "code": 200,
  "data": {
    "skillList": [
      {
        "skillID": 1,
        "skillName": "技能名称"
      }
    ]
  },
  "msg": "ok"
}
```

## 机器人数据接口

这些接口需要 token，并按管理员或绑定关系校验权限。

### 获取机器人当前人格

路由：`GET /api/robot/{robotID}/personality`

是否需要 token：是

返回体：

```json
{
  "code": 200,
  "data": {
    "mbti": {
      "E": 88,
      "I": 12,
      "S": 12,
      "N": 88,
      "T": 12,
      "F": 88,
      "J": 45,
      "P": 55
    },
    "big5": {
      "neuroticism": 50,
      "extraversion": 50,
      "openness": 50,
      "agreeableness": 50,
      "conscientiousness": 50
    }
  },
  "msg": "ok"
}
```

说明：MBTI 四组相对字段分别求和为 100。

### 修改机器人人格

路由：`PUT /api/robot/{robotID}/personality/change`

是否需要 token：是

请求体：

```json
{
  "robotID": "ROBOT-DEMO-001",
  "mbti": {
    "E": 88,
    "I": 12,
    "S": 12,
    "N": 88,
    "T": 12,
    "F": 88,
    "J": 45,
    "P": 55
  },
  "big5": {
    "neuroticism": 50,
    "extraversion": 50,
    "openness": 50,
    "agreeableness": 50,
    "conscientiousness": 50
  }
}
```

返回体：

```json
{
  "code": 200,
  "data": null,
  "msg": "ok"
}
```

### 修改机器人音色

路由：`PUT /api/robot/{robotID}/tone/change`

是否需要 token：是

请求体：

```json
{
  "robotID": "ROBOT-DEMO-001",
  "toneID": 1
}
```

返回体：

```json
{
  "code": 200,
  "data": null,
  "msg": "ok"
}
```

### 获取机器人消息记录

路由：`GET /api/robot/{robotID}/message/{cursor}/{limit}`

是否需要 token：是

分页说明：

- `cursor=0` 表示从最新记录开始。
- `cursor` 非 0 时查询 `created_at < cursor` 的历史记录。
- `limit` 表示本次最多返回条数。

返回体：

```json
{
  "code": 200,
  "data": {
    "messageList": [
      {
        "messageID": "message-uuid",
        "createdAt": 1710000000000,
        "content": "原始消息内容",
        "belong": "userID-or-robotID"
      }
    ],
    "meta": {
      "nextCursor": "1710000000000",
      "hasMore": true
    }
  },
  "msg": "ok"
}
```

### 获取机器人摘要

路由：`GET /api/robot/{robotID}/abstract/{cursor}/{limit}`

是否需要 token：是

分页规则同消息记录接口。

返回体：

```json
{
  "code": 200,
  "data": {
    "abstractList": [
      {
        "abstractID": "summary-uuid",
        "createdAt": 1710000000000,
        "content": "摘要内容"
      }
    ],
    "meta": {
      "nextCursor": "1710000000000",
      "hasMore": true
    }
  },
  "msg": "ok"
}
```

### 获取机器人用户画像列表

路由：`GET /api/robot/{robotID}/userportrait`

是否需要 token：是

返回体：

```json
{
  "code": 200,
  "data": {
    "userPortraitList": [
      {
        "portraitID": "portrait-uuid",
        "avatar": "用户画像头像路径"
      }
    ]
  },
  "msg": "ok"
}
```

### 查看机器人具体用户画像

路由：`GET /api/robot/{robotID}/userportrait/{portraitID}`

是否需要 token：是

返回体：

```json
{
  "code": 200,
  "data": {
    "userPortrait": {
      "portraitID": "portrait-uuid",
      "avatar": "用户画像头像路径",
      "createdAt": 1710000000000,
      "updatedAt": 1710000000000,
      "userName": "用户昵称",
      "age": 12,
      "profession": "用户职业",
      "mbti": {
        "E": 88,
        "I": 12,
        "S": 12,
        "N": 88,
        "T": 12,
        "F": 88,
        "J": 45,
        "P": 55
      },
      "big5": {
        "neuroticism": 50,
        "extraversion": 50,
        "openness": 50,
        "agreeableness": 50,
        "conscientiousness": 50
      },
      "familyTies": "用户的家庭关系"
    }
  },
  "msg": "ok"
}
```

### 获取机器人家庭画像

路由：`GET /api/robot/{robotID}/familyportrait`

是否需要 token：是

返回体：

```json
{
  "code": 200,
  "data": {
    "familyPortrait": {
      "familyID": "family-uuid",
      "createdAt": 1710000000000,
      "updatedAt": 1710000000000,
      "content": "家庭画像内容"
    }
  },
  "msg": "ok"
}
```
