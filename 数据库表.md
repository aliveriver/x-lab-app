# 数据库表设计

## 设计结论

你列的 **用户表、机器人表、消息表、摘要表、人格表、音色表、技能表** 可以覆盖核心数据，但还不够完整。

原因是接口里还存在绑定关系、技能启用关系、人格历史、用户画像、家庭画像这些业务数据。它们如果直接塞进机器人表或用户表，后面查询、分页、解绑、历史追踪都会比较难维护。

建议最终保留 12 张表：

| 表名 | 中文名 | 说明 |
| --- | --- | --- |
| `users` | 用户表 | 手机号、昵称、头像等用户基础信息 |
| `robots` | 机器人表 | 机器人编码、默认名称、当前音色等 |
| `user_robot_bindings` | 用户机器人绑定表 | 用户和机器人的绑定关系、用户给机器人的昵称 |
| `messages` | 消息表 | 原始对话数据 |
| `summaries` | 摘要表 | 对话摘要，对应接口里的 `abstractList` |
| `personality_presets` | 初始人格表 | 系统预设人格，对应“获取全部机器人初始人格” |
| `robot_personality_records` | 机器人人格记录表 | 当前人格和历史人格，支持“动态人格” |
| `tones` | 音色表 | 可选 TTS 音色 |
| `skills` | 技能表 | 可选技能 |
| `robot_skills` | 机器人技能表 | 机器人已启用的技能 |
| `user_portraits` | 用户画像表 | 家庭成员/用户画像详情 |
| `family_portraits` | 家庭画像表 | 机器人对应的家庭整体画像 |

## 统一约定

- 接口字段继续使用小驼峰，例如 `userID`、`createdAt`。
- 数据库字段统一使用下划线连接，例如 `user_id`、`created_at`。
- 所有时间字段统一使用 13 位毫秒时间戳，类型为 `BIGINT UNSIGNED`。
- 每张表都包含 `created_at`、`updated_at`、`deleted_at`。
- `deleted_at` 为 `NULL` 表示未删除；有值表示软删除时间。
- 用户、消息、摘要、画像等业务对象使用 UUID，类型为 `CHAR(36)`。
- 机器人 ID 按接口含义，直接使用机器人编码，类型为 `VARCHAR(64)`。
- MBTI 建议使用 `E/I`、`S/N`、`T/F`、`J/P` 八个字段。接口示例里 `T` 后面写了 `N`，这里应改为 `F`，因为 `N` 已经用于 `S/N` 维度。
- SQL 按 MySQL 8.0 / InnoDB / utf8mb4 设计。关联字段只建索引，不建数据库外键，关系一致性由后端业务层处理。

## 索引设计

| 表名 | 主要索引 | 用途 |
| --- | --- | --- |
| `users` | `uk_users_phone_number` | 手机号登录/注册 |
| `robots` | 主键 `robot_id` | 通过机器人编码查询 |
| `user_robot_bindings` | `idx_bindings_user`、`idx_bindings_robot`、`uk_bindings_active_pair` | 查询用户机器人列表、防止重复绑定 |
| `messages` | `idx_messages_robot_created`、`idx_messages_user_robot_created` | 分页查询机器人原始对话 |
| `summaries` | `idx_summaries_robot_created` | 分页查询机器人摘要 |
| `personality_presets` | `idx_personality_presets_enabled` | 查询可用初始人格 |
| `robot_personality_records` | `uk_robot_personality_current`、`idx_robot_personality_history` | 查询当前人格和人格历史 |
| `tones` | `idx_tones_enabled` | 查询可用音色 |
| `skills` | `idx_skills_enabled` | 查询可用技能 |
| `robot_skills` | `idx_robot_skills_robot`、`uk_robot_skills_active_pair` | 查询机器人技能、防止重复启用 |
| `user_portraits` | `idx_user_portraits_robot` | 查询机器人下的用户画像 |
| `family_portraits` | `uk_family_portrait_current` | 查询机器人当前家庭画像 |

## 表结构

### 1. `users` 用户表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `user_id` | `CHAR(36)` | 用户 UUID，接口中的 `userID` |
| `phone_number` | `VARCHAR(20)` | 手机号 |
| `user_name` | `VARCHAR(64)` | 用户名 |
| `avatar` | `VARCHAR(512)` | 用户头像路径 |
| `created_at` | `BIGINT UNSIGNED` | 创建时间，13 位时间戳 |
| `updated_at` | `BIGINT UNSIGNED` | 更新时间，13 位时间戳 |
| `deleted_at` | `BIGINT UNSIGNED NULL` | 删除时间，13 位时间戳 |

### 2. `robots` 机器人表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `robot_id` | `VARCHAR(64)` | 机器人编码，接口中的 `robotID` / `robotCode` |
| `robot_name` | `VARCHAR(64)` | 机器人默认名称 |
| `model` | `VARCHAR(64)` | 机器人型号 |
| `current_tone_id` | `BIGINT UNSIGNED NULL` | 当前音色 ID |
| `status` | `TINYINT UNSIGNED` | 状态：1 正常，2 停用 |
| `created_at` | `BIGINT UNSIGNED` | 创建时间 |
| `updated_at` | `BIGINT UNSIGNED` | 更新时间 |
| `deleted_at` | `BIGINT UNSIGNED NULL` | 删除时间 |

### 3. `user_robot_bindings` 用户机器人绑定表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `binding_id` | `CHAR(36)` | 绑定记录 UUID |
| `user_id` | `CHAR(36)` | 用户 ID |
| `robot_id` | `VARCHAR(64)` | 机器人编码 |
| `robot_alias` | `VARCHAR(64)` | 用户给机器人的昵称，对应返回里的 `robotName` |
| `init_personality_id` | `BIGINT UNSIGNED NULL` | 绑定时选择的初始人格 ID |
| `bind_tone_id` | `BIGINT UNSIGNED NULL` | 绑定时选择的音色 ID |
| `created_at` | `BIGINT UNSIGNED` | 创建时间 |
| `updated_at` | `BIGINT UNSIGNED` | 更新时间 |
| `deleted_at` | `BIGINT UNSIGNED NULL` | 解绑时间 |

### 4. `messages` 消息表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `message_id` | `CHAR(36)` | 消息 UUID，接口中的 `messageID` |
| `robot_id` | `VARCHAR(64)` | 所属机器人 |
| `user_id` | `CHAR(36) NULL` | 关联用户，没有明确用户时可为空 |
| `speaker_type` | `ENUM('user','robot','system')` | 说话方类型 |
| `speaker_id` | `VARCHAR(64)` | 说话方 ID，对应接口中的 `belong` |
| `content` | `TEXT` | 原始消息内容 |
| `message_type` | `VARCHAR(32)` | 消息类型，如 `text`、`audio` |
| `extra` | `JSON NULL` | 扩展信息 |
| `created_at` | `BIGINT UNSIGNED` | 消息创建时间 |
| `updated_at` | `BIGINT UNSIGNED` | 更新时间 |
| `deleted_at` | `BIGINT UNSIGNED NULL` | 删除时间 |

### 5. `summaries` 摘要表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `summary_id` | `CHAR(36)` | 摘要 UUID，返回接口时可映射为 `abstractID` |
| `robot_id` | `VARCHAR(64)` | 所属机器人 |
| `user_id` | `CHAR(36) NULL` | 关联用户 |
| `strategy` | `ENUM('time','round','manual','other')` | 摘要策略：时间、轮数、人工、其他 |
| `start_message_id` | `CHAR(36) NULL` | 摘要起始消息 |
| `end_message_id` | `CHAR(36) NULL` | 摘要结束消息 |
| `content` | `TEXT` | 摘要内容 |
| `extra` | `JSON NULL` | 扩展信息 |
| `created_at` | `BIGINT UNSIGNED` | 创建时间 |
| `updated_at` | `BIGINT UNSIGNED` | 更新时间 |
| `deleted_at` | `BIGINT UNSIGNED NULL` | 删除时间 |

### 6. `personality_presets` 初始人格表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `personality_id` | `BIGINT UNSIGNED` | 初始人格 ID |
| `personality_name` | `VARCHAR(64)` | 初始人格名称 |
| `description` | `TEXT NULL` | 初始人格描述 |
| `mbti_e` / `mbti_i` | `TINYINT UNSIGNED` | E/I 维度，建议两者和为 100 |
| `mbti_s` / `mbti_n` | `TINYINT UNSIGNED` | S/N 维度，建议两者和为 100 |
| `mbti_t` / `mbti_f` | `TINYINT UNSIGNED` | T/F 维度，建议两者和为 100 |
| `mbti_j` / `mbti_p` | `TINYINT UNSIGNED` | J/P 维度，建议两者和为 100 |
| `big5_neuroticism` | `TINYINT UNSIGNED` | 神经质 |
| `big5_extraversion` | `TINYINT UNSIGNED` | 外倾性 |
| `big5_openness` | `TINYINT UNSIGNED` | 开放性 |
| `big5_agreeableness` | `TINYINT UNSIGNED` | 宜人性 |
| `big5_conscientiousness` | `TINYINT UNSIGNED` | 尽责性 |
| `enabled` | `TINYINT UNSIGNED` | 是否启用 |
| `created_at` | `BIGINT UNSIGNED` | 创建时间 |
| `updated_at` | `BIGINT UNSIGNED` | 更新时间 |
| `deleted_at` | `BIGINT UNSIGNED NULL` | 删除时间 |

### 7. `robot_personality_records` 机器人人格记录表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `personality_record_id` | `CHAR(36)` | 人格记录 UUID |
| `robot_id` | `VARCHAR(64)` | 所属机器人 |
| `source` | `ENUM('preset','manual','analysis')` | 来源：预设、手动修改、后端分析 |
| `preset_personality_id` | `BIGINT UNSIGNED NULL` | 来源预设人格 ID |
| `is_current` | `TINYINT UNSIGNED` | 是否当前人格 |
| `mbti_*` | `TINYINT UNSIGNED` | MBTI 各维度 |
| `big5_*` | `TINYINT UNSIGNED` | 大五人格各维度 |
| `created_at` | `BIGINT UNSIGNED` | 创建时间 |
| `updated_at` | `BIGINT UNSIGNED` | 更新时间 |
| `deleted_at` | `BIGINT UNSIGNED NULL` | 删除时间 |

### 8. `tones` 音色表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `tone_id` | `BIGINT UNSIGNED` | 音色 ID |
| `tone_name` | `VARCHAR(64)` | 音色名称 |
| `provider` | `VARCHAR(64)` | TTS 服务商 |
| `voice_code` | `VARCHAR(128)` | 服务商侧音色编码 |
| `demo_url` | `VARCHAR(512)` | 试听地址 |
| `description` | `TEXT NULL` | 描述 |
| `enabled` | `TINYINT UNSIGNED` | 是否启用 |
| `created_at` | `BIGINT UNSIGNED` | 创建时间 |
| `updated_at` | `BIGINT UNSIGNED` | 更新时间 |
| `deleted_at` | `BIGINT UNSIGNED NULL` | 删除时间 |

### 9. `skills` 技能表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `skill_id` | `BIGINT UNSIGNED` | 技能 ID |
| `skill_name` | `VARCHAR(64)` | 技能名称 |
| `skill_type` | `ENUM('board','llm','system','other')` | 技能类型 |
| `description` | `TEXT NULL` | 技能描述 |
| `config_schema` | `JSON NULL` | 技能配置结构 |
| `enabled` | `TINYINT UNSIGNED` | 是否启用 |
| `created_at` | `BIGINT UNSIGNED` | 创建时间 |
| `updated_at` | `BIGINT UNSIGNED` | 更新时间 |
| `deleted_at` | `BIGINT UNSIGNED NULL` | 删除时间 |

### 10. `robot_skills` 机器人技能表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `robot_skill_id` | `CHAR(36)` | 机器人技能记录 UUID |
| `robot_id` | `VARCHAR(64)` | 机器人编码 |
| `skill_id` | `BIGINT UNSIGNED` | 技能 ID |
| `skill_config` | `JSON NULL` | 该机器人使用技能时的配置 |
| `enabled` | `TINYINT UNSIGNED` | 是否启用 |
| `created_at` | `BIGINT UNSIGNED` | 创建时间 |
| `updated_at` | `BIGINT UNSIGNED` | 更新时间 |
| `deleted_at` | `BIGINT UNSIGNED NULL` | 删除时间 |

### 11. `user_portraits` 用户画像表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `portrait_id` | `CHAR(36)` | 画像 UUID，接口中的 `portraitID` |
| `robot_id` | `VARCHAR(64)` | 所属机器人 |
| `avatar` | `VARCHAR(512)` | 画像头像路径 |
| `user_name` | `VARCHAR(64)` | 用户昵称 |
| `age` | `TINYINT UNSIGNED NULL` | 年龄 |
| `profession` | `VARCHAR(128)` | 职业 |
| `education_level` | `VARCHAR(64)` | 受教育程度 |
| `family_ties` | `VARCHAR(255)` | 家庭关系 |
| `dialogue_style` | `TEXT NULL` | 对话风格 |
| `mbti_*` | `TINYINT UNSIGNED` | MBTI 各维度 |
| `big5_*` | `TINYINT UNSIGNED` | 大五人格各维度 |
| `extra` | `JSON NULL` | 扩展画像信息 |
| `created_at` | `BIGINT UNSIGNED` | 创建时间 |
| `updated_at` | `BIGINT UNSIGNED` | 更新时间 |
| `deleted_at` | `BIGINT UNSIGNED NULL` | 删除时间 |

### 12. `family_portraits` 家庭画像表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `family_id` | `CHAR(36)` | 家庭画像 UUID |
| `robot_id` | `VARCHAR(64)` | 所属机器人 |
| `content` | `TEXT` | 家庭画像内容 |
| `is_current` | `TINYINT UNSIGNED` | 是否当前画像 |
| `extra` | `JSON NULL` | 扩展信息 |
| `created_at` | `BIGINT UNSIGNED` | 创建时间 |
| `updated_at` | `BIGINT UNSIGNED` | 更新时间 |
| `deleted_at` | `BIGINT UNSIGNED NULL` | 删除时间 |

## SQL 建表语句

```sql
CREATE DATABASE IF NOT EXISTS `x_lab_app`
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;

USE `x_lab_app`;

CREATE TABLE IF NOT EXISTS `users` (
  `user_id` CHAR(36) NOT NULL COMMENT '用户 UUID，对应接口 userID',
  `phone_number` VARCHAR(20) NOT NULL COMMENT '手机号',
  `user_name` VARCHAR(64) NOT NULL DEFAULT '' COMMENT '用户名',
  `avatar` VARCHAR(512) NOT NULL DEFAULT '' COMMENT '用户头像路径',
  `created_at` BIGINT UNSIGNED NOT NULL COMMENT '创建时间，13位毫秒时间戳',
  `updated_at` BIGINT UNSIGNED NOT NULL COMMENT '更新时间，13位毫秒时间戳',
  `deleted_at` BIGINT UNSIGNED NULL COMMENT '删除时间，13位毫秒时间戳，NULL表示未删除',
  PRIMARY KEY (`user_id`),
  UNIQUE KEY `uk_users_phone_number` (`phone_number`),
  KEY `idx_users_deleted_at` (`deleted_at`),
  CONSTRAINT `chk_users_created_at_13` CHECK (`created_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_users_updated_at_13` CHECK (`updated_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_users_deleted_at_13` CHECK (`deleted_at` IS NULL OR `deleted_at` BETWEEN 1000000000000 AND 9999999999999)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';

CREATE TABLE IF NOT EXISTS `tones` (
  `tone_id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '音色ID',
  `tone_name` VARCHAR(64) NOT NULL COMMENT '音色名称',
  `provider` VARCHAR(64) NOT NULL DEFAULT '' COMMENT 'TTS服务商',
  `voice_code` VARCHAR(128) NOT NULL DEFAULT '' COMMENT '服务商侧音色编码',
  `demo_url` VARCHAR(512) NOT NULL DEFAULT '' COMMENT '试听地址',
  `description` TEXT NULL COMMENT '音色描述',
  `enabled` TINYINT UNSIGNED NOT NULL DEFAULT 1 COMMENT '是否启用：1启用，0停用',
  `created_at` BIGINT UNSIGNED NOT NULL COMMENT '创建时间，13位毫秒时间戳',
  `updated_at` BIGINT UNSIGNED NOT NULL COMMENT '更新时间，13位毫秒时间戳',
  `deleted_at` BIGINT UNSIGNED NULL COMMENT '删除时间，13位毫秒时间戳，NULL表示未删除',
  PRIMARY KEY (`tone_id`),
  KEY `idx_tones_enabled` (`enabled`, `deleted_at`),
  CONSTRAINT `chk_tones_enabled` CHECK (`enabled` IN (0, 1)),
  CONSTRAINT `chk_tones_created_at_13` CHECK (`created_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_tones_updated_at_13` CHECK (`updated_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_tones_deleted_at_13` CHECK (`deleted_at` IS NULL OR `deleted_at` BETWEEN 1000000000000 AND 9999999999999)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='音色表';

CREATE TABLE IF NOT EXISTS `skills` (
  `skill_id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '技能ID',
  `skill_name` VARCHAR(64) NOT NULL COMMENT '技能名称',
  `skill_type` ENUM('board', 'llm', 'system', 'other') NOT NULL DEFAULT 'llm' COMMENT '技能类型',
  `description` TEXT NULL COMMENT '技能描述',
  `config_schema` JSON NULL COMMENT '技能配置结构',
  `enabled` TINYINT UNSIGNED NOT NULL DEFAULT 1 COMMENT '是否启用：1启用，0停用',
  `created_at` BIGINT UNSIGNED NOT NULL COMMENT '创建时间，13位毫秒时间戳',
  `updated_at` BIGINT UNSIGNED NOT NULL COMMENT '更新时间，13位毫秒时间戳',
  `deleted_at` BIGINT UNSIGNED NULL COMMENT '删除时间，13位毫秒时间戳，NULL表示未删除',
  PRIMARY KEY (`skill_id`),
  KEY `idx_skills_enabled` (`enabled`, `deleted_at`),
  CONSTRAINT `chk_skills_enabled` CHECK (`enabled` IN (0, 1)),
  CONSTRAINT `chk_skills_created_at_13` CHECK (`created_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_skills_updated_at_13` CHECK (`updated_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_skills_deleted_at_13` CHECK (`deleted_at` IS NULL OR `deleted_at` BETWEEN 1000000000000 AND 9999999999999)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='技能表';

CREATE TABLE IF NOT EXISTS `personality_presets` (
  `personality_id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '初始人格ID',
  `personality_name` VARCHAR(64) NOT NULL COMMENT '初始人格名称',
  `description` TEXT NULL COMMENT '初始人格描述',
  `mbti_e` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI E',
  `mbti_i` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI I',
  `mbti_s` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI S',
  `mbti_n` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI N',
  `mbti_t` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI T',
  `mbti_f` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI F',
  `mbti_j` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI J',
  `mbti_p` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI P',
  `big5_neuroticism` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：神经质',
  `big5_extraversion` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：外倾性',
  `big5_openness` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：开放性',
  `big5_agreeableness` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：宜人性',
  `big5_conscientiousness` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：尽责性',
  `enabled` TINYINT UNSIGNED NOT NULL DEFAULT 1 COMMENT '是否启用：1启用，0停用',
  `created_at` BIGINT UNSIGNED NOT NULL COMMENT '创建时间，13位毫秒时间戳',
  `updated_at` BIGINT UNSIGNED NOT NULL COMMENT '更新时间，13位毫秒时间戳',
  `deleted_at` BIGINT UNSIGNED NULL COMMENT '删除时间，13位毫秒时间戳，NULL表示未删除',
  PRIMARY KEY (`personality_id`),
  KEY `idx_personality_presets_enabled` (`enabled`, `deleted_at`),
  CONSTRAINT `chk_personality_presets_mbti_range` CHECK (
    `mbti_e` <= 100 AND `mbti_i` <= 100 AND `mbti_s` <= 100 AND `mbti_n` <= 100
    AND `mbti_t` <= 100 AND `mbti_f` <= 100 AND `mbti_j` <= 100 AND `mbti_p` <= 100
  ),
  CONSTRAINT `chk_personality_presets_big5_range` CHECK (
    `big5_neuroticism` <= 100 AND `big5_extraversion` <= 100 AND `big5_openness` <= 100
    AND `big5_agreeableness` <= 100 AND `big5_conscientiousness` <= 100
  ),
  CONSTRAINT `chk_personality_presets_created_at_13` CHECK (`created_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_personality_presets_updated_at_13` CHECK (`updated_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_personality_presets_deleted_at_13` CHECK (`deleted_at` IS NULL OR `deleted_at` BETWEEN 1000000000000 AND 9999999999999)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='初始人格表';

CREATE TABLE IF NOT EXISTS `robots` (
  `robot_id` VARCHAR(64) NOT NULL COMMENT '机器人编码，对应接口 robotID/robotCode',
  `robot_name` VARCHAR(64) NOT NULL DEFAULT '' COMMENT '机器人默认名称',
  `model` VARCHAR(64) NOT NULL DEFAULT '' COMMENT '机器人型号',
  `current_tone_id` BIGINT UNSIGNED NULL COMMENT '当前音色ID',
  `status` TINYINT UNSIGNED NOT NULL DEFAULT 1 COMMENT '状态：1正常，2停用',
  `created_at` BIGINT UNSIGNED NOT NULL COMMENT '创建时间，13位毫秒时间戳',
  `updated_at` BIGINT UNSIGNED NOT NULL COMMENT '更新时间，13位毫秒时间戳',
  `deleted_at` BIGINT UNSIGNED NULL COMMENT '删除时间，13位毫秒时间戳，NULL表示未删除',
  PRIMARY KEY (`robot_id`),
  KEY `idx_robots_tone` (`current_tone_id`),
  KEY `idx_robots_deleted_at` (`deleted_at`),
  CONSTRAINT `chk_robots_status` CHECK (`status` IN (1, 2)),
  CONSTRAINT `chk_robots_created_at_13` CHECK (`created_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_robots_updated_at_13` CHECK (`updated_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_robots_deleted_at_13` CHECK (`deleted_at` IS NULL OR `deleted_at` BETWEEN 1000000000000 AND 9999999999999)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='机器人表';

CREATE TABLE IF NOT EXISTS `user_robot_bindings` (
  `binding_id` CHAR(36) NOT NULL COMMENT '绑定记录UUID',
  `user_id` CHAR(36) NOT NULL COMMENT '用户ID',
  `robot_id` VARCHAR(64) NOT NULL COMMENT '机器人编码',
  `robot_alias` VARCHAR(64) NOT NULL DEFAULT '' COMMENT '用户给机器人的昵称',
  `init_personality_id` BIGINT UNSIGNED NULL COMMENT '绑定时选择的初始人格ID',
  `bind_tone_id` BIGINT UNSIGNED NULL COMMENT '绑定时选择的音色ID',
  `created_at` BIGINT UNSIGNED NOT NULL COMMENT '创建时间，13位毫秒时间戳',
  `updated_at` BIGINT UNSIGNED NOT NULL COMMENT '更新时间，13位毫秒时间戳',
  `deleted_at` BIGINT UNSIGNED NULL COMMENT '解绑时间，13位毫秒时间戳，NULL表示仍绑定',
  `active_pair_key` VARCHAR(140) GENERATED ALWAYS AS (
    CASE WHEN `deleted_at` IS NULL THEN CONCAT(`user_id`, '#', `robot_id`) ELSE NULL END
  ) STORED COMMENT '未删除绑定的唯一键辅助字段',
  PRIMARY KEY (`binding_id`),
  UNIQUE KEY `uk_bindings_active_pair` (`active_pair_key`),
  KEY `idx_bindings_user` (`user_id`, `deleted_at`),
  KEY `idx_bindings_robot` (`robot_id`, `deleted_at`),
  KEY `idx_bindings_init_personality` (`init_personality_id`),
  KEY `idx_bindings_tone` (`bind_tone_id`),
  CONSTRAINT `chk_bindings_created_at_13` CHECK (`created_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_bindings_updated_at_13` CHECK (`updated_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_bindings_deleted_at_13` CHECK (`deleted_at` IS NULL OR `deleted_at` BETWEEN 1000000000000 AND 9999999999999)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户机器人绑定表';

CREATE TABLE IF NOT EXISTS `robot_personality_records` (
  `personality_record_id` CHAR(36) NOT NULL COMMENT '机器人人格记录UUID',
  `robot_id` VARCHAR(64) NOT NULL COMMENT '机器人编码',
  `source` ENUM('preset', 'manual', 'analysis') NOT NULL DEFAULT 'manual' COMMENT '来源：预设、手动修改、后端分析',
  `preset_personality_id` BIGINT UNSIGNED NULL COMMENT '来源预设人格ID',
  `is_current` TINYINT UNSIGNED NOT NULL DEFAULT 1 COMMENT '是否当前人格：1是，0否',
  `mbti_e` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI E',
  `mbti_i` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI I',
  `mbti_s` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI S',
  `mbti_n` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI N',
  `mbti_t` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI T',
  `mbti_f` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI F',
  `mbti_j` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI J',
  `mbti_p` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI P',
  `big5_neuroticism` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：神经质',
  `big5_extraversion` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：外倾性',
  `big5_openness` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：开放性',
  `big5_agreeableness` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：宜人性',
  `big5_conscientiousness` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：尽责性',
  `created_at` BIGINT UNSIGNED NOT NULL COMMENT '创建时间，13位毫秒时间戳',
  `updated_at` BIGINT UNSIGNED NOT NULL COMMENT '更新时间，13位毫秒时间戳',
  `deleted_at` BIGINT UNSIGNED NULL COMMENT '删除时间，13位毫秒时间戳，NULL表示未删除',
  `current_robot_id` VARCHAR(64) GENERATED ALWAYS AS (
    CASE WHEN `is_current` = 1 AND `deleted_at` IS NULL THEN `robot_id` ELSE NULL END
  ) STORED COMMENT '当前人格唯一键辅助字段',
  PRIMARY KEY (`personality_record_id`),
  UNIQUE KEY `uk_robot_personality_current` (`current_robot_id`),
  KEY `idx_robot_personality_history` (`robot_id`, `created_at`, `deleted_at`),
  KEY `idx_robot_personality_preset` (`preset_personality_id`),
  CONSTRAINT `chk_robot_personality_current` CHECK (`is_current` IN (0, 1)),
  CONSTRAINT `chk_robot_personality_mbti_range` CHECK (
    `mbti_e` <= 100 AND `mbti_i` <= 100 AND `mbti_s` <= 100 AND `mbti_n` <= 100
    AND `mbti_t` <= 100 AND `mbti_f` <= 100 AND `mbti_j` <= 100 AND `mbti_p` <= 100
  ),
  CONSTRAINT `chk_robot_personality_big5_range` CHECK (
    `big5_neuroticism` <= 100 AND `big5_extraversion` <= 100 AND `big5_openness` <= 100
    AND `big5_agreeableness` <= 100 AND `big5_conscientiousness` <= 100
  ),
  CONSTRAINT `chk_robot_personality_created_at_13` CHECK (`created_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_robot_personality_updated_at_13` CHECK (`updated_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_robot_personality_deleted_at_13` CHECK (`deleted_at` IS NULL OR `deleted_at` BETWEEN 1000000000000 AND 9999999999999)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='机器人人格记录表';

CREATE TABLE IF NOT EXISTS `robot_skills` (
  `robot_skill_id` CHAR(36) NOT NULL COMMENT '机器人技能记录UUID',
  `robot_id` VARCHAR(64) NOT NULL COMMENT '机器人编码',
  `skill_id` BIGINT UNSIGNED NOT NULL COMMENT '技能ID',
  `skill_config` JSON NULL COMMENT '该机器人使用技能时的配置',
  `enabled` TINYINT UNSIGNED NOT NULL DEFAULT 1 COMMENT '是否启用：1启用，0停用',
  `created_at` BIGINT UNSIGNED NOT NULL COMMENT '创建时间，13位毫秒时间戳',
  `updated_at` BIGINT UNSIGNED NOT NULL COMMENT '更新时间，13位毫秒时间戳',
  `deleted_at` BIGINT UNSIGNED NULL COMMENT '删除时间，13位毫秒时间戳，NULL表示未删除',
  `active_pair_key` VARCHAR(96) GENERATED ALWAYS AS (
    CASE WHEN `deleted_at` IS NULL THEN CONCAT(`robot_id`, '#', `skill_id`) ELSE NULL END
  ) STORED COMMENT '未删除机器人技能唯一键辅助字段',
  PRIMARY KEY (`robot_skill_id`),
  UNIQUE KEY `uk_robot_skills_active_pair` (`active_pair_key`),
  KEY `idx_robot_skills_robot` (`robot_id`, `enabled`, `deleted_at`),
  KEY `idx_robot_skills_skill` (`skill_id`, `deleted_at`),
  CONSTRAINT `chk_robot_skills_enabled` CHECK (`enabled` IN (0, 1)),
  CONSTRAINT `chk_robot_skills_created_at_13` CHECK (`created_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_robot_skills_updated_at_13` CHECK (`updated_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_robot_skills_deleted_at_13` CHECK (`deleted_at` IS NULL OR `deleted_at` BETWEEN 1000000000000 AND 9999999999999)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='机器人技能表';

CREATE TABLE IF NOT EXISTS `messages` (
  `message_id` CHAR(36) NOT NULL COMMENT '消息UUID，对应接口 messageID',
  `robot_id` VARCHAR(64) NOT NULL COMMENT '机器人编码',
  `user_id` CHAR(36) NULL COMMENT '关联用户ID',
  `speaker_type` ENUM('user', 'robot', 'system') NOT NULL COMMENT '说话方类型',
  `speaker_id` VARCHAR(64) NOT NULL COMMENT '说话方ID，对应接口 belong',
  `content` TEXT NOT NULL COMMENT '原始消息内容',
  `message_type` VARCHAR(32) NOT NULL DEFAULT 'text' COMMENT '消息类型：text/audio/image等',
  `extra` JSON NULL COMMENT '扩展信息',
  `created_at` BIGINT UNSIGNED NOT NULL COMMENT '消息创建时间，13位毫秒时间戳',
  `updated_at` BIGINT UNSIGNED NOT NULL COMMENT '更新时间，13位毫秒时间戳',
  `deleted_at` BIGINT UNSIGNED NULL COMMENT '删除时间，13位毫秒时间戳，NULL表示未删除',
  PRIMARY KEY (`message_id`),
  KEY `idx_messages_robot_created` (`robot_id`, `created_at`, `message_id`),
  KEY `idx_messages_user_robot_created` (`user_id`, `robot_id`, `created_at`),
  KEY `idx_messages_deleted_at` (`deleted_at`),
  CONSTRAINT `chk_messages_created_at_13` CHECK (`created_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_messages_updated_at_13` CHECK (`updated_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_messages_deleted_at_13` CHECK (`deleted_at` IS NULL OR `deleted_at` BETWEEN 1000000000000 AND 9999999999999)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='消息表';

CREATE TABLE IF NOT EXISTS `summaries` (
  `summary_id` CHAR(36) NOT NULL COMMENT '摘要UUID，接口返回时可映射为 abstractID',
  `robot_id` VARCHAR(64) NOT NULL COMMENT '机器人编码',
  `user_id` CHAR(36) NULL COMMENT '关联用户ID',
  `strategy` ENUM('time', 'round', 'manual', 'other') NOT NULL DEFAULT 'other' COMMENT '摘要策略',
  `start_message_id` CHAR(36) NULL COMMENT '摘要起始消息ID',
  `end_message_id` CHAR(36) NULL COMMENT '摘要结束消息ID',
  `content` TEXT NOT NULL COMMENT '摘要内容',
  `extra` JSON NULL COMMENT '扩展信息',
  `created_at` BIGINT UNSIGNED NOT NULL COMMENT '创建时间，13位毫秒时间戳',
  `updated_at` BIGINT UNSIGNED NOT NULL COMMENT '更新时间，13位毫秒时间戳',
  `deleted_at` BIGINT UNSIGNED NULL COMMENT '删除时间，13位毫秒时间戳，NULL表示未删除',
  PRIMARY KEY (`summary_id`),
  KEY `idx_summaries_robot_created` (`robot_id`, `created_at`, `summary_id`),
  KEY `idx_summaries_user_robot_created` (`user_id`, `robot_id`, `created_at`),
  KEY `idx_summaries_message_range` (`start_message_id`, `end_message_id`),
  CONSTRAINT `chk_summaries_created_at_13` CHECK (`created_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_summaries_updated_at_13` CHECK (`updated_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_summaries_deleted_at_13` CHECK (`deleted_at` IS NULL OR `deleted_at` BETWEEN 1000000000000 AND 9999999999999)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='摘要表';

CREATE TABLE IF NOT EXISTS `user_portraits` (
  `portrait_id` CHAR(36) NOT NULL COMMENT '用户画像UUID，对应接口 portraitID',
  `robot_id` VARCHAR(64) NOT NULL COMMENT '机器人编码',
  `avatar` VARCHAR(512) NOT NULL DEFAULT '' COMMENT '画像头像路径',
  `user_name` VARCHAR(64) NOT NULL DEFAULT '' COMMENT '用户昵称',
  `age` TINYINT UNSIGNED NULL COMMENT '年龄',
  `profession` VARCHAR(128) NOT NULL DEFAULT '' COMMENT '职业',
  `education_level` VARCHAR(64) NOT NULL DEFAULT '' COMMENT '受教育程度',
  `family_ties` VARCHAR(255) NOT NULL DEFAULT '' COMMENT '家庭关系',
  `dialogue_style` TEXT NULL COMMENT '对话风格',
  `mbti_e` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI E',
  `mbti_i` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI I',
  `mbti_s` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI S',
  `mbti_n` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI N',
  `mbti_t` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI T',
  `mbti_f` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI F',
  `mbti_j` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI J',
  `mbti_p` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT 'MBTI P',
  `big5_neuroticism` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：神经质',
  `big5_extraversion` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：外倾性',
  `big5_openness` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：开放性',
  `big5_agreeableness` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：宜人性',
  `big5_conscientiousness` TINYINT UNSIGNED NOT NULL DEFAULT 50 COMMENT '大五人格：尽责性',
  `extra` JSON NULL COMMENT '扩展画像信息',
  `created_at` BIGINT UNSIGNED NOT NULL COMMENT '创建时间，13位毫秒时间戳',
  `updated_at` BIGINT UNSIGNED NOT NULL COMMENT '更新时间，13位毫秒时间戳',
  `deleted_at` BIGINT UNSIGNED NULL COMMENT '删除时间，13位毫秒时间戳，NULL表示未删除',
  PRIMARY KEY (`portrait_id`),
  KEY `idx_user_portraits_robot` (`robot_id`, `deleted_at`),
  KEY `idx_user_portraits_created` (`created_at`),
  CONSTRAINT `chk_user_portraits_mbti_range` CHECK (
    `mbti_e` <= 100 AND `mbti_i` <= 100 AND `mbti_s` <= 100 AND `mbti_n` <= 100
    AND `mbti_t` <= 100 AND `mbti_f` <= 100 AND `mbti_j` <= 100 AND `mbti_p` <= 100
  ),
  CONSTRAINT `chk_user_portraits_big5_range` CHECK (
    `big5_neuroticism` <= 100 AND `big5_extraversion` <= 100 AND `big5_openness` <= 100
    AND `big5_agreeableness` <= 100 AND `big5_conscientiousness` <= 100
  ),
  CONSTRAINT `chk_user_portraits_created_at_13` CHECK (`created_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_user_portraits_updated_at_13` CHECK (`updated_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_user_portraits_deleted_at_13` CHECK (`deleted_at` IS NULL OR `deleted_at` BETWEEN 1000000000000 AND 9999999999999)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户画像表';

CREATE TABLE IF NOT EXISTS `family_portraits` (
  `family_id` CHAR(36) NOT NULL COMMENT '家庭画像UUID',
  `robot_id` VARCHAR(64) NOT NULL COMMENT '机器人编码',
  `content` TEXT NOT NULL COMMENT '家庭画像内容',
  `is_current` TINYINT UNSIGNED NOT NULL DEFAULT 1 COMMENT '是否当前画像：1是，0否',
  `extra` JSON NULL COMMENT '扩展信息',
  `created_at` BIGINT UNSIGNED NOT NULL COMMENT '创建时间，13位毫秒时间戳',
  `updated_at` BIGINT UNSIGNED NOT NULL COMMENT '更新时间，13位毫秒时间戳',
  `deleted_at` BIGINT UNSIGNED NULL COMMENT '删除时间，13位毫秒时间戳，NULL表示未删除',
  `current_robot_id` VARCHAR(64) GENERATED ALWAYS AS (
    CASE WHEN `is_current` = 1 AND `deleted_at` IS NULL THEN `robot_id` ELSE NULL END
  ) STORED COMMENT '当前家庭画像唯一键辅助字段',
  PRIMARY KEY (`family_id`),
  UNIQUE KEY `uk_family_portrait_current` (`current_robot_id`),
  KEY `idx_family_portraits_robot_created` (`robot_id`, `created_at`, `deleted_at`),
  CONSTRAINT `chk_family_portraits_current` CHECK (`is_current` IN (0, 1)),
  CONSTRAINT `chk_family_portraits_created_at_13` CHECK (`created_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_family_portraits_updated_at_13` CHECK (`updated_at` BETWEEN 1000000000000 AND 9999999999999),
  CONSTRAINT `chk_family_portraits_deleted_at_13` CHECK (`deleted_at` IS NULL OR `deleted_at` BETWEEN 1000000000000 AND 9999999999999)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='家庭画像表';
```

## 接口字段映射建议

| 接口字段 | 数据库字段 |
| --- | --- |
| `userID` | `users.user_id` |
| `phonenumber` | `users.phone_number` |
| `userName` | `users.user_name` |
| `robotID` | `robots.robot_id` |
| `robotCode` | `robots.robot_id` |
| `robotName` | `user_robot_bindings.robot_alias`，为空时可用 `robots.robot_name` |
| `messageID` | `messages.message_id` |
| `belong` | `messages.speaker_id` |
| `abstractID` | `summaries.summary_id` |
| `personalityID` | `personality_presets.personality_id` |
| `toneID` | `tones.tone_id` |
| `skillID` | `skills.skill_id` |
| `portraitID` | `user_portraits.portrait_id` |
| `familyID` | `family_portraits.family_id` |
| `createdAt` | `created_at` |
| `updatedAt` | `updated_at` |
| `deletedAt` | `deleted_at` |

## 后续可选表

当前接口没有强制要求，但如果后端自己管理验证码和登录态，可以再补两张表：

| 表名 | 用途 |
| --- | --- |
| `sms_codes` | 存短信验证码、过期时间、使用状态 |
| `user_sessions` | 存 token、刷新时间、失效时间 |

如果 token 使用 JWT，验证码接第三方服务且不需要审计，这两张表可以暂时不建。


